{
  "enabled": {
    "type": "bool",
    "default": true,
    "description": "Enable phone tools"
  },
  "adb_path": {
    "type": "text",
    "default": "adb",
    "description": "ADB executable path"
  },
  "adb_serial": {
    "type": "text",
    "default": "",
    "description": "Phone ADB serial"
  },
  "health_db_path": {
    "type": "text",
    "default": "",
    "description": "Read-only SQLite path produced by xiaomi-sync (usually .../data/health.db)"
  },
  "operit_base_url": {
    "type": "text",
    "default": "http://127.0.0.1:8094",
    "description": "Phone Operit external HTTP base URL"
  },
  "operit_token": {
    "type": "text",
    "default": "",
    "description": "Bearer token for Operit external HTTP service"
  },
  "operit_timeout_seconds": {
    "type": "int",
    "default": 120,
    "description": "Operit task timeout"
  },
  "control_backend": {
    "type": "text",
    "default": "operit",
    "description": "Phone control backend: operit or adb"
  },
  "app_aliases_json": {
    "type": "text",
    "default": "{}",
    "description": "Optional JSON map of friendly app names to Android package names"
  },
  "sleep_guard_exempt_apps": {
    "type": "text",
    "default": "",
    "description": "Apps excluded from on-demand temporary policies"
  },
  "audit_log_path": {
    "type": "text",
    "default": "phone_agent_audit.jsonl",
    "description": "Local metadata-only action audit log"
  },
  "reminders_path": {
    "type": "text",
    "default": "phone_agent_reminders.json",
    "description": "Persistent phone-agent reminder file"
  },
  "policy_state_path": {
    "type": "text",
    "default": "phone_agent_policies.json",
    "description": "Persistent app-policy state used to restore temporary restrictions after restart"
  },
  "adb_full_port_scan": {
    "type": "bool",
    "default": true,
    "description": "Scan all TCP ports when recovering optional ADB diagnostics"
  },
  "allowed_user_ids": {
    "type": "text",
    "default": "",
    "description": "Comma-separated AstrBot user IDs allowed to invoke phone actions"
  },
  "use_private_companion_auth": {
    "type": "bool",
    "default": false,
    "description": "Optional: use Private Companion authorization when allowlist is empty"
  },
  "command_timeout_seconds": {
    "type": "int",
    "default": 20,
    "description": "ADB command timeout"
  },
  "sleep_guard_packages": {
    "type": "text",
    "default": "",
    "description": "Comma-separated app aliases or package names for on-demand temporary policies (empty disables the default list)"
  },
  "relay_base_url": {
    "type": "text",
    "default": "",
    "description": "Public relay base URL (deploy/relay) used when Operit over Tailscale is unreachable, e.g. http://[240e:...]:8888/phone-relay"
  },
  "relay_token": {
    "type": "text",
    "default": "",
    "description": "Shared bearer token for the relay queue"
  },
  "enable_health_tools": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_health tool and /health route (requires xiaomi-sync health.db)"
  },
  "max_background_tasks": {
    "type": "int",
    "default": 2,
    "description": "Maximum concurrent background Operit tasks"
  },
  "tasks_path": {
    "type": "text",
    "default": "phone_agent_tasks.json",
    "description": "Persistent metadata file for background Operit tasks"
  },
  "enable_observe_tool": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_observe, which asks Operit to inspect the current app and screen"
  },
  "enable_usage_tool": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_usage tool (reads Android app usage through Operit)"
  },
  "enable_location_tool": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_location tool and /location route (sensitive)"
  },
  "enable_reminder_tools": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_reminder tool and /reminders route"
  },
  "enable_policy_tools": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_app_policy, phone_sleep_mode, /app_policy and /sleep_mode, plus suspend actions in phone_action"
  },
  "enable_audit_tool": {
    "type": "bool",
    "default": false,
    "description": "Enable phone_audit tool and /audit route"
  }
}
